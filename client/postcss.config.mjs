// Minimal PostCSS config to avoid loading Tailwind-specific plugins in this demo
export default {
  plugins: {
    // no-op plugins; keep an empty config so Vite doesn't search for Tailwind deps
  }
};
